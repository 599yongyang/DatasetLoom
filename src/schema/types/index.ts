export enum ProjectRole {
    OWNER = 'OWNER',
    ADMIN = 'ADMIN',
    EDITOR = 'EDITOR',
    VIEWER = 'VIEWER'
}

export enum UserRole {
    USER = 'USER',
    ADMIN = 'ADMIN'
}

export enum ChatVisibilityType {
    PUBLIC = 'PUBLIC',
    PRIVATE = 'PRIVATE'
}
